import React, {Component} from "react";
import {
    FlatList,
    Image,
    StyleSheet,
    Text,
    TextInput,
    ScrollView,
    View
} from 'react-native';

export default class RestaurantInfo extends Component {
    render() {
        return (
            <View>
                <Text>Info</Text>
            </View>
        )
    }
}